// Global Vitest setup. Kept intentionally minimal — each spec stubs the browser
// APIs (Image, etc.) and installs the Vue plugins (i18n / router / pinia) it
// actually needs, so there is no hidden shared state between specs.
export {}
